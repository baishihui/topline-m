import Vue from 'vue'

import { Button, NavBar, Cell, CellGroup, Icon, Field, Toast, CountDown } from 'vant'

Vue.use(Button)
  .use(NavBar)
  .use(Cell)
  .use(Icon)
  .use(CellGroup)
  .use(Field)
  .use(Toast)
  .use(CountDown)
